# Raw ParlaMint files (local only)

Place manually acquired ParlaMint 5.0 Spanish TEI/XML files here.

See `docs/sources/parlamint.md` for acquisition instructions and licence warnings.

Expected files:

- `ParlaMint-ES-listPerson.xml`
- `ParlaMint-ES-listOrg.xml`
- One or more session `ParlaMint-ES_*.xml` component files (not `*.ana.xml`)

This directory is gitignored; contents are not committed.
