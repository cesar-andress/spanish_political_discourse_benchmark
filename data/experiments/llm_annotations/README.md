# LLM annotation experiment outputs (generated by `make llm-annotation-local`).

Local model runs write JSONL files here, e.g.:

```
data/experiments/llm_annotations/llama-local_pilot_100.jsonl
```

Reports are written under `reports/llm_annotation/`.
